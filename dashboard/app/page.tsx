export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "64px", maxWidth: 720 }}>
      <h1>Manifest</h1>
      <p>
        Broker dashboard scaffold. Active-load views, the agent audit trail, the
        approvals queue, and the analytics view land in Phase 7 — see the repo
        README for the full build plan.
      </p>
    </main>
  );
}
